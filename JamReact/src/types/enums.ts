export enum DifficultyLevel {
	Easy = 0,
	Medium = 1,
	Hard = 2,
}

export enum Accessibility {
	Public = 0,
	Private = 1,
}

export enum SceneType {
	Intro = 0,
	Question = 1,
	Ending = 2,
}

export enum EndingType {
	Good = 0,
	Neutral = 1,
	Bad = 2,
}
